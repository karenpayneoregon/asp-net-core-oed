@page
@model Demo1.Pages.ConfigurationModel
@{
}

<div class="card">
    <div class="card-header">
        Configuration
    </div>
    <div class="card-body">
        <h5 class="card-title">Administrator page</h5>
        <p class="card-text">Place all configuration items here</p>
        <a href="#" class="btn btn-primary">Save</a> &nbsp;<a href="#" class="btn btn-secondary">Cancel</a>
    </div>
</div>